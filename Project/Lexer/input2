/* input2
 * 
 * same program as input1, except with more extensive COMMENTS
 * (see input2.out for results)
 *
 * author: Huizhe Li, Master Student, Dartmouth College
 *
 */

#include <stdio.h>				

extern int integer;				

int main(void){	
	/* a huge wave of comments coming... */				
	static/**/const/**/int/**/array[/*r u kidding*/3/*i m not*/] = {/*wow..*/0/*come on*/,/*leami alone*/0,0}/*soon*/;
	struct bozo{				
		int/* come up
wi-
th a really cool name!
*/ bo;
		double /* 'bo' is not 
good
enough...*/zo;
	}/* but 'zo' is much better */;

	struct bozo bz;
	bz.bo = /* play with
bo*/1;
	bz.zo -= 1.0/* or with zo?
*
*/;	

	integer/*aha...integers*/ += /*how much space*/sizeof/*do u take?*****/(/* lets 
*/int/*see*/);		
	/* playing with integers...
..
...
..

..

.
.

.
.

.
*/
	integer /= integer * 3 / 4 - 5;		
	integer *= integer;	
	/* let's see what conditions have to say */		
	if/*if waht?*/ ( (integer < integer + 1)/*and?...
*****/ && (integer <= 0)){ /* then? */				
		if ((--integer/*stupid expression */ > -1)/*or...?*/ || (/*saying the obvious!*/++integer >= integer/*i'm do-
ne with this mess!!!!!
*/))/*hope it's a closure*/{/*oh no..*/			
				if ((integer == !integer)/* make no sense*/ &&/* just for fun*/ (integer != integer)){	
					printf/* finally sth interesting
*/("---string literal---\nanother line!\n"/*i dont think a newline is coming up*/);
					integer = /*hello*/10/*i'm*/%/*MOD! haha*/
10 + &integer;			
				}
		}
	}else{	/* half way, hold on to it */										
		do{									
			switch/*oh Mr. Switch is joining us*/ (integer){ 					
				case 0: 	/* bringing with him his child Case 
*/						
					continue/* they continue to annoy us */;				
				default:/* yeah they do that by default */						
			}/* Ser Jorah Break
coming to save us!
*/
			break;/* break the dawn */								
		}while(1);	/* while bringing the light! */							

	}
	/* congratulations u have survived, input2
let's return to normal life */
	return 0;									
}
	


