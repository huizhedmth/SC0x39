#include<stdio.h>

int f();

int a = 1;
int b = 2;
const static const const const int volatile volatile const volatile x =1;
int main(void)
{
	const static const const const int y = 1;
	f();
	switch(a+b);

	printf("test\n");

	a;
	int const d;
	int const x = 0, xx = 0, xxx = 0;
	printf("%d\n", x);
	return 0;
}

int f()
{
	return 1;
}
